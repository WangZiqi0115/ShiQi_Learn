# Claude 每日必读

每天与示其的第一次对话前，先阅读本文件，按其中约定执行。

---

## 一、项目基本信息

- 用户称呼：**示其**
- 项目总名：**示其的逆袭计划**
- 最终目标：**浙江大学计算机学硕**（408 统考）
- 大学：重庆理工大学 · 数学与应用数学专业（2026 级）
- **当前主线：大一上结束转专业到两江人工智能学院「人工智能」（2026.9 起）**
- **当前阶段**：转专业准备（GPA 为主）＋ Python→MNIST 手写识别项目(Day 27)＋ Linux 20 天(Day 2)＋ C 语言(9 月下旬开课)
- 竞赛主线：数学建模（未来），辅助：编程竞赛

## 二、学习计划规则（Python / Linux 通用）

1. **每天都有学习内容**：Python 从 Day 23 起围绕 MNIST 重排；Linux 按 20 天推进
2. **按天编号推进**：Python Day 23 = 2026.9.5，以此类推；Linux Day 1 = 2026.9.4
3. **文件夹格式**：`dayN (日期)`，如 `day23 (2026.9.5)`
4. **每天生成要素**：知识点 + 练习（Python 另加 `代码测试专用.py`；Linux 为 知识点.md + 练习.md）
5. **不图快，每天只深入一个主题**：内容宁可少而精，不要为了赶进度跳坑
6. **Python 已重排为 MNIST 项目导向**：砍掉标准库/爬虫/数据库，改为 NumPy → 机器学习概念 → 从零搭神经网络(784→64→10)，详见 [AI数字识别小项目.md](示其的逆袭计划/AI数字识别小项目.md)

## 三、更新日志时必须同步这几处（无需提醒）

| 文件 | 内容 |
|------|------|
| 最外层 `README.md` | 简略总览：各项目进度、每日状态表 |
| `示其的逆袭计划/Python学习/Python学习日志.md` | 详细：项目结构、进度表、每日问题记录、Git 提交记录 |
| `示其的逆袭计划/AI数字识别小项目.md` | Python 重排计划（MNIST 导向） |
| `示其的逆袭计划/Linux学习/Linux学习日志.md` | Linux 20 天进度 |
| `示其的逆袭计划/转专业到人工智能.md` | 转专业计划 |

用户说"更新日志/上传"时，这些一起改，再 git commit + push。

## 四、出题与练习规范

1. **"请修改"后面只写题目描述，不写答案**，练习题保持 None/空
2. 用户做完练习后叫检查，逐题点评错误，用户自己修改后复查
3. 预估分数：统计会 FAIL 的题，给出"预估 X/总分"
4. 用户提问时耐心解释，用类比帮助理解
5. **当用户提问时，如果问题能在今天的知识点文件中找到对应内容，执行顺序：先在对话框中给出完整解答 → 再在源文件对应位置注释标注 → 最后告诉用户标注的具体位置（章节/行号）**
6. **定期回顾用户问过的问题，把已问过的解答补进知识点文件（标注时同样告知具体位置）**
7. **每天知识点文件末尾必须加"例题精讲"**：至少 2-4 道，每道包含题目、思路、参考代码、运行输出、关键点；先看懂例题再做练习
7. **示例数据必须和对应代码放到一起，别嫌麻烦**：讲嵌套结构、报错示例、函数讲解等内容时，把 `students`、`score_list`、`matrix` 这类示例数据直接写在对应代码上方或旁边；不能只写 `students[0]` 却不给出 `students` 长什么样，宁可重复也不要让用户来回翻文件
8. **新函数讲解规范（参数顺序标注法）**：遇到用户没学过的函数（内置函数或标准库函数，如 `functools.reduce`、`sorted` 等），用 `函数名(①参数1, ②参数2, ③可选参数)` 的写法标注括号里每个参数的位置与含义，并在代码示例旁同步写"【怎么读】"注释；让用户一眼看清每个位置该写什么，对话框中讲解时同样用这个格式
9. **每天练习末尾加一道"回顾题"**：每天练习文件最后（"检验标准"之前）加 **1 道题**，专门**回顾以前 Day 1-22 的语法地基**（list/dict/函数/生成器/正则/闭包/装饰器/bytes 等；因为 Day 23 起已切进 NumPy/MNIST 新赛道，回顾题专守旧语法这条线，防断档）。**每天过一部分，也可综合几天出一道**。对 Python 课与 Linux 课都适用，题目出自 Day 22 及以前的任一 Day（不是当天新内容，也不是 Day 23 之后的 NumPy 题）。建议按日期倒序轮换（Day 24 回顾 Day 22，Day 25 回顾 Day 21…），每 3-4 天穿插一道"综合几天"的题，到 Day 1 循环。
10. **生成/修改练习题后，必须跑"答案泄漏检查"（2026.9.7 新增，硬规矩）**：因为反复出现"把答案写进练习正文或提示"的错误，现将 `check_no_answer.py` 定为强制工具。**凡是生成或修改 `*练习*.py`，完成后一律运行**：
    ```bash
    py "示其的逆袭计划/Python学习/check_no_answer.py" "示其的逆袭计划/Python学习/dayN (日期)/xxx练习.py"
    ```
    输出 `[通过]` 才能提交/交付；输出 `[警告]` 就说明提示/标记行里混进了**能直接运行的答案代码**（如 `print(A @ B)`、`h = W1@x+b1`），必须清掉再跑一次，直到通过。检查器只扫"提示/请在下方/期望/任务/答案"这些引导行，且放过 `?` 占位模板（fill-in-the-blank 不算泄漏），**不会误伤题目自带的数据、也不会误判纯文字提示**（如"矩阵乘用 @"、"转置用 .T"）。注意：`check_no_answer.py` 相对路径是从 `E:\学习\python学习\` 的外层开始算；不确定时用绝对路径，或先 `cd` 到 `Python学习\` 再从那里运行。

## 五、Git 工作流

- 每次学习完成后：`git add -A` → `git commit` → `git push`
- 临时测试文件（test.txt、示例文章.txt、text.txt 等）清理掉并加入 .gitignore，不进仓库
- 沙箱推送失败时，提醒用户在 VS Code 终端里执行 `git push`
- **补充知识点注释时不需要每次 git 同步**：当天中途只改文件不提交推送
- **只在两个节点推送并说明提交内容**：
  1. 每天生成学习内容时（创建后 commit + push，说明新增了什么）
  2. 每天学习完成更新日志时（三份文档 + 当日文件一起 commit + push，说明完成内容）

## 六、当前进度快照

| 项目 | 进度 | 说明 |
|------|------|------|
| 转专业到人工智能 | 🎯 大一上结束申请 | 计划见 转专业到人工智能.md |
| Python | ✅ Day 1-26（语法＋NumPy 基础）｜🔥 Day 27（机器学习概念）起 MNIST 导向 | 进度见 Python学习日志 |
| Linux | ✅ Day 1-2 | 20 天计划 |
| C 语言 | ⏳ 9 月下旬开课 | 随学校课程走 |
| Office | ✅ Day 2 | — |

（进度表需随每日学习情况更新）

## 七、用户易错点记录（讲解时重点提醒）

- `append`/`remove`/赋值语句返回 None，需要先操作再 return
- 方法调用不能忘 `()`：`word.lower()` 而不是 `word.lower`
- 遍历解包变量名要对上：`for name, score in d.items()`
- 修改字典用 `d[key]=v`，不要整体重新赋值
- 索引从 0 数：第 n 个元素是 `parts[n-1]`
- 文件对象只能用一次遍历，读多次先存进列表
- `f.write()` 只接收一个字符串参数，写多行要加 `\n`
- 排序前想清楚升降序：`reverse=True`
- 石头剪刀布胜负逻辑：石头>剪刀>布>石头

## 八、文件修改与 VS Code 同步约定（重要）

**背景（2026.8.14）**：Codex 修改了 VS Code 正打开的文件，VS Code 缓存未刷新，用户保存时把 Codex 新加的注释覆盖掉了。

**约定：**

1. **Codex 修改文件后**：如果该文件正在 VS Code 中打开，先按 `Ctrl+Shift+P` → `Revert File`（或关掉标签页重新打开），再继续阅读/修改
2. **不要在有旧缓存时按 Ctrl+S 保存**，否则会用旧内容覆盖 Codex 的新内容
3. VS Code 弹出 "The file has been changed on disk" 时，一律选 **Revert / 恢复**（不要选 Overwrite）
4. 重要改动后可先本地 `git commit` 一次作为安全网（不推送也可以），万一被覆盖可从 git 找回
